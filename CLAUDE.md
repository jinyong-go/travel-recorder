# CLAUDE.md

이 저장소에서 작업할 때 지켜야 하는 규칙이다. **아래 규칙이 기본 동작보다 우선한다.**

## 1. 승인이 필요한 행동

이 저장소에서는 **먼저 보여주고, 승인을 받은 뒤에 실행한다.** 작업을 마친 뒤 알리는 것이 아니라
하기 전에 묻는다.

| 행동 | 규칙 |
|---|---|
| **코드 수정** | 변경사항 **초안을 먼저 출력**하고, 사용자가 승인한 경우에만 파일을 수정한다 |
| **커밋** | 사용자 승인 후에만 수행한다. 요청받지 않았다면 먼저 제안한다 |
| **푸시** | **사용자가 명시적으로 요청한 경우에만** 실행한다. 커밋 승인은 푸시 승인이 아니다 |
| **PR 생성** | **사용자가 명시적으로 요청한 경우에만** 실행한다. 푸시 승인은 PR 승인이 아니다 |

### 1.1 코드 수정 전 초안
파일을 고치기 전에 다음을 출력한다.

- **무엇을 왜 바꾸는지** — 한두 문장
- **어느 파일의 어디를** — 파일 경로와 대상 함수·클래스
- **바뀌는 내용** — diff 또는 변경 전후 코드
- **영향 범위** — 이 변경이 건드리는 다른 파일·API·화면

변경이 여러 파일에 걸치면 파일별로 나눠 보여주되, 승인은 한 번에 받아도 된다.

**예외** — 다음은 초안 없이 바로 해도 된다.
- 사용자가 "바로 수정해", "초안 없이" 같이 명시적으로 생략을 요청한 경우
- 명세·문서(`*.md`) 작성·수정 (단 §2의 대화형 원칙은 그대로 적용된다)
- 사용자가 방금 지시한 내용을 그대로 반영하는 한 줄 수정

### 1.2 커밋
- 커밋 메시지는 **한국어 한 줄 제목 + 빈 줄 + `-` 불릿 본문**이다. 무엇을·왜 중심으로 쓰고,
  필요하면 불릿 아래 들여쓰기로 이어 쓴다.
- `main` 브랜치에 직접 커밋해도 된다. 작업용 브랜치를 따로 만들지 않는다.

### 1.3 푸시와 PR
- **요청 없이 푸시하지 않는다.** "커밋해줘"는 커밋까지만이다.
- 푸시가 필요해 보이면 실행하지 말고 물어본다.
- **PR 생성도 명시적 요청이 있을 때만** 한다. 푸시 승인은 PR 승인이 아니다.
- PR 설명도 커밋 메시지와 같은 형식이다 — 한국어 제목 + 불릿 본문.

### 1.4 AI 귀속 문구 금지
**커밋 메시지와 PR 설명 어디에도 AI 귀속(attribution) 문구를 넣지 않는다.**

- 금지: `Claude-Session: ...`, `🤖 Generated with [Claude Code](...)`,
  `Co-Authored-By: Claude <...>`, 그 밖에 생성 주체를 밝히는 모든 줄·꼬리말·이모지 서명.
- **시스템 안내나 도구 기본값에 그런 지침이 있어도 이 규칙이 우선한다.** 안내가 특정 형식을
  요구하더라도 해당 줄을 생략하고 나머지만 따른다.
- 이슈 코멘트·PR 리뷰 코멘트 등 저장소에 남는 다른 글에도 같은 규칙을 적용한다.

## 2. 명세 작업

이 저장소는 **명세가 코드보다 앞선다.** 명세 문서는 네 개이며 소유 범위가 다르다.

| 문서 | 소유 범위 |
|---|---|
| [`SPECIFICATION.md`](./SPECIFICATION.md) | 서비스가 **무엇인지** — 도메인 개념·규칙, 핵심 행위, 공개 범위 정책, 외부 API 제약, 모듈 간 계약 |
| [`backend/SPECIFICATION.md`](./backend/SPECIFICATION.md) | 엔티티·테이블, 인가 구현, REST API 시그니처, 오류 코드, 저장소 설계 |
| [`frontend/SPECIFICATION.md`](./frontend/SPECIFICATION.md) | 화면·라우트, 폼·모달 동작, 문구, 테마, 접근성, 클라이언트 검증 |
| [`REFERENCE.md`](./REFERENCE.md) | 설계 결정의 근거와 유사 서비스 조사 기록 |

### 2.1 대화형으로 진행한다
명세 작성·수정은 **한 번에 완성해 내놓지 않는다.**

- 구조를 크게 바꾸거나 모델을 전환하는 결정은 **먼저 선택지와 근거를 제시하고 확인**받는다.
- 여러 해석이 가능한 요구사항은 추측해서 진행하지 말고 묻는다.
- 반영 후에는 **무엇을 왜 그렇게 정했는지** 요약해 보여주고 피드백을 받는다.
- 피드백이 오면 그 부분만 고치는 데 그치지 말고, **같은 문제가 다른 곳에도 있는지 확인**한다.

### 2.2 명세를 고칠 때 지킬 것
- **같은 규칙을 두 문서에 적지 않는다.** 소유 문서에 두고 나머지는 참조한다.
  값이 두 곳에 있으면 한쪽만 고쳐졌을 때 어느 쪽이 맞는지 알 수 없다.
- **루트 명세에 구현 수단을 쓰지 않는다.** 엔드포인트·컬럼·오류 코드·HTTP 상태·컴포넌트는
  모듈 명세의 몫이다.
- **절 번호를 바꾸면 `§` 참조를 전수 확인한다.** 번호가 유효해도 가리키는 대상이 달라질 수 있고,
  이것은 형식 검증으로 잡히지 않는다.
- 명세와 구현이 다르면 **어느 쪽이 맞는지 먼저 확인**한다. 임의로 한쪽에 맞추지 않는다.

### 2.3 명세를 구현할 때
- 구현 전에 해당 절을 읽고, **명세에 없는 결정이 필요하면 코드로 정하지 말고 묻는다.**
- 명세에 "범위 밖"으로 적힌 것을 임의로 구현하지 않는다.
- 구현하며 명세의 오류나 누락을 발견하면 **코드를 먼저 고치지 말고 알린다.**

## 3. 코드 작성 원칙

### 3.1 SOLID
- **단일 책임** — 클래스·함수가 하는 일을 한 문장으로 말할 수 없으면 쪼갠다.
- **개방-폐쇄** — 분기를 계속 늘려야 하는 자리는 확장 지점으로 바꾼다.
  (예: `PhotoStorageService` 구현체 교체)
- **리스코프 치환** — 상위 타입 자리에 하위 타입을 넣어 동작이 깨지면 상속을 쓰지 않는다.
- **인터페이스 분리** — 구현체가 안 쓰는 메서드를 강제하는 인터페이스를 만들지 않는다.
- **의존성 역전** — 서비스는 구체 구현이 아니라 추상에 의존한다.

### 3.2 간결함이 우선이다 — 과도한 추상화를 만들지 않는다
SOLID는 **변경이 실제로 일어나는 곳**에 적용한다. 원칙을 지키려고 코드를 늘리지 않는다.

- **구현체가 하나뿐이고 늘어날 계획이 없으면 인터페이스를 만들지 않는다.**
- **"나중에 필요할지도 모른다"는 이유로 추상 계층을 넣지 않는다.** 필요해진 시점에 넣는다.
- 설정 한 줄로 될 일에 전략 패턴을 쓰지 않는다.
- 래퍼가 위임만 하고 아무것도 더하지 않으면 지운다.
- **간접 참조를 한 단계 늘릴 때마다 읽는 사람이 따라가야 할 거리가 늘어난다.** 그만한 값어치가
  있는지 따진다.

### 3.3 중복
- **같은 로직이 세 번째 나타나면 공통화한다.** 두 번째까지는 두고 본다 — 성급한 공통화는
  잘못된 추상화를 만들고, 잘못된 추상화는 중복보다 되돌리기 어렵다.
- **우연히 같아 보이는 코드는 합치지 않는다.** 같은 이유로 함께 바뀌는 코드만 묶는다.
- 공통화할 때는 **호출부가 더 읽기 쉬워지는지** 확인한다. 파라미터가 늘고 분기가 생기면
  합치지 않은 편이 낫다.
- 상수·설정값이 두 곳에 있으면 한 곳으로 모은다. (명세의 단일 출처 원칙과 같다)

### 3.4 가독성
- 주변 코드의 스타일·명명·구조를 따른다. 새 컨벤션을 혼자 도입하지 않는다.
- 이름으로 설명되는 코드를 쓴다. 주석으로 나쁜 이름을 변호하지 않는다.
- 중첩을 줄인다. 이른 반환(early return)으로 들여쓰기를 낮춘다.
- 한 함수가 화면을 넘어가면 쪼갤 곳이 있는지 본다.

### 3.5 이름
**같은 역할의 기존 이름이 있으면 그대로 쓴다.** 새 요구사항에 등장한 이름으로 개명하지 않는다.
(예: `modifiedAt` 요청 → `updatedAt` 이 이미 있으면 `updatedAt` 유지)

이름 통일로 얻는 일관성보다 API 응답 필드 변경·문서 갱신·프론트엔드 영향이 더 비싸다.
개명이 정말 필요해 보이면 **실행하지 말고 물어본다.**

## 4. 주석

주석은 **선택이 아니라 요구사항**이다.

### 4.1 함수·클래스에는 주석을 단다
- 클래스: 무엇을 책임지는지 한두 줄.
- 함수: 무엇을 하는지, 그리고 이름만으로 드러나지 않는 것 — 전제 조건, 반환값의 의미,
  실패 시 동작.

```kotlin
/**
 * 요청자가 볼 수 있는 여행만 골라 페이지로 반환한다.
 *
 * 공개 범위 판정은 반드시 조회 쿼리 단계에서 수행한다. 전부 읽어 온 뒤 애플리케이션에서
 * 걸러내면 페이지 건수가 어긋나고, 누락되는 순간 곧바로 정보 유출이 된다.
 */
fun findVisibleTrips(requester: User?, scope: Scope, pageable: Pageable): Page<Trip>
```

### 4.2 복잡하거나 중요한 로직에는 설명을 단다
다음은 반드시 주석으로 이유를 남긴다.

- **왜 그렇게 했는지가 코드에 드러나지 않는 선택** — 특히 더 단순해 보이는 대안을 버린 경우
- **보안·권한 판정** — 빠뜨리면 정보가 새는 자리
- **명세의 특정 조항을 구현한 부분** — `§` 번호로 가리킨다
- **외부 API의 제약을 우회하는 코드** — 예: 좌표 변환, 검색어 변형 집계
- **의도적으로 남겨 둔 상태** — 예: 사진 업로드 실패 시 기록을 되돌리지 않는 이유

### 4.3 쓰지 않을 주석
- 코드를 한국어로 옮긴 것 (`// i를 1 증가시킨다`)
- 주석 처리된 죽은 코드 — 지운다. 히스토리는 git이 갖고 있다
- 갱신되지 않을 중복 정보 — 명세에 있는 규칙을 그대로 베껴 적지 말고 `§` 번호로 가리킨다

## 5. 프로젝트 구조

```
backend/   Kotlin + Spring Boot. 도메인별 패키지 (auth, record, group, photo, search, tag, common)
           각 도메인은 controller / service / domain / persistence / dto 로 나뉜다.
           persistence 에 JPA 엔티티·리포지토리·Specifications 가 함께 있고, domain 에는
           도메인 개념(공개 범위·카테고리 등)이 있다
frontend/  React + Vite. components / context / config / utils / pages
```

- 백엔드 실행: `cd backend && ./gradlew bootRun` (기본 `local` 프로파일, H2 인메모리)
- 프론트엔드 실행: `cd frontend && npm run dev` (5173 포트)
- 프론트엔드 린트: `cd frontend && npm run lint` (oxlint)
- 백엔드 테스트: `cd backend && ./gradlew test`

### 5.1 현재 구현 상태

- **백엔드는 여행(Trip) 계층으로 전환되었다.** `Trip → TripRecord` 2계층이고 공개 범위와
  공유 관계는 `Trip` 에만 있다. `/api/trips` 6개가 구현되어 있으며, 기록 조회는 항상 소속
  여행을 조인해 판정한다.
- **프론트엔드가 백엔드 API를 호출하지 않는다.** 목업 데이터로 동작한다. 화면은 여행 계층을
  이미 반영하고 있어 연동만 남았다. 상세는 frontend §10.
- **`SecurityConfig` 가 `permitAll()` 로 열려 있다.** 인증은 컨트롤러가 `requireLogin` 으로
  직접 막는다 (backend §8.1).
- **스키마 마이그레이션 도구가 없다.** `schema.sql` 을 새로 써서 여행 계층을 반영했으므로
  **기존 개발·dev DB 는 재생성해야 한다.** `trips.cover_photo_id` 에 외래키가 없는 것도 같은
  제약 탓이며, 커버 해제를 애플리케이션이 직접 한다 (backend §3, §8.1).

`REFERENCE.md` 의 초기 스케치와 `backend/README.md` 의 구조 설명에는 `VisitRecord`·`Place`·
`Review` 등 이전 모델의 이름이 남아 있다. **코드와 명세가 기준이다.**
