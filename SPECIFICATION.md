# 여행 지도 (travel-recorder) 공통 요구사항 명세서

## 0. 이 문서의 위치

본 문서는 백엔드·프론트엔드가 **함께 지켜야 하는 규칙**만 정의한다. 한쪽 모듈에서만
의미를 갖는 내용(화면 구조·컴포넌트·라우트, 엔티티 컬럼·프레임워크 설정·엔드포인트 시그니처)은
각 모듈 명세에 둔다.

| 문서 | 다루는 범위 |
|---|---|
| 본 문서 (`SPECIFICATION.md`) | 서비스 정의, 용어, 도메인 개념, 공개 범위 정책, 모듈 간 연동 계약, 외부 API 제약 |
| [`backend/SPECIFICATION.md`](./backend/SPECIFICATION.md) | 도메인 모델(엔티티), 인가 구현, REST API 명세, 사진 저장소 설계, 오류 처리 구현 |
| [`frontend/SPECIFICATION.md`](./frontend/SPECIFICATION.md) | 화면·라우트, 폼·모달 동작, 테마, 접근성, 클라이언트 검증 |

**충돌 시 본 문서가 우선한다.** 모듈 명세는 본 문서의 규칙을 구체화할 수는 있어도 뒤집을 수 없다.

> **이 개정판은 [공개 범위 및 공유 설계 검토](./REFERENCE.md)의 결론을 반영한다.** 이전 판의
> "모두가 공유하는 여행지 + 여러 사용자의 리뷰" 모델은 폐기되었고, **개인 소유의 방문 기록 +
> 공개 범위** 모델로 대체되었다. 변경의 배경과 유사 서비스 조사 결과는 REFERENCE.md를 참고한다.
> 코드도 이 모델로 전환되어 있다. 남은 차이는 §8에 정리한다.

## 1. 서비스 개요

### 1.1 목적
사용자가 다녀온 여행 장소를 **개인 기록으로 남기고**, 원하는 상대에게만 선택적으로 공유하는
웹 애플리케이션이다. 네이버 계정으로 로그인한 사용자가 방문한 장소에 사진·메모·평점을 남기며,
각 기록의 공개 범위는 작성자가 직접 정한다. 기본값은 비공개다.

### 1.2 구성
프론트엔드(React + Vite)와 백엔드(Kotlin + Spring Boot)가 분리된 2모듈 구조이며,
브라우저 ↔ 백엔드는 JSON REST API + 세션 쿠키로 통신한다 (§6).

### 1.3 용어

| 용어 | 정의 |
|---|---|
| 방문 기록 (VisitRecord) | 사용자가 방문한 장소 1건에 대한 **개인 기록**. 서비스의 유일한 저장 단위다 |
| 작성자 | 방문 기록을 만든 사용자. 편집 권한은 항상 작성자에게만 있다 |
| 공개 범위 (visibility) | 기록을 누가 볼 수 있는지. `PRIVATE` / `GROUP` / `PUBLIC` 중 하나 |
| 그룹 | 반복 재사용하는 **조회 전용 공유 대상 목록**. 편집 권한과는 무관하다 |
| 그룹 소유자 | 그룹을 만든 사용자. 멤버 초대·제외·그룹 삭제 권한을 가진다 |
| 멤버 | 그룹에 속한 사용자. 그 그룹으로 공유된 기록을 **볼 수만** 있다 |
| 카테고리 | 관광지 / 쇼핑 / 맛집 3종 중 하나 (단일 선택, 필수) |
| 태그 | 카테고리 외에 기록을 설명하는 자유 형식 키워드 (다중, 선택) |
| 평점 (rating) | 기록에 남기는 0.5~5.0 점수. 기록당 1개이며 작성자 본인의 평가다 |
| 메모 (memo) | 기록에 남기는 자유 텍스트 설명 |
| 사진 | 기록에 첨부된 이미지 파일. 기록에 종속된다 |
| 장소 | 외부 검색으로 조회한 지점 정보. **자체 저장 단위가 아니며** 기록에 스냅샷으로 복사된다 |
| 기준 위치 | 거리순 정렬과 장소 검색 정렬의 기준이 되는 사용자 위치 (§5) |

### 1.4 카테고리 값

| 표시명 | 코드 |
|---|---|
| 관광지 | `SIGHT` |
| 쇼핑 | `SHOPPING` |
| 맛집 | `FOOD` |

API 요청·응답은 코드값을 쓰고, 화면 표기는 프론트엔드가 매핑한다. 목록 필터의 "전체"는
카테고리 미지정을 의미하며 별도 코드값이 아니다.

### 1.5 이전 모델과의 차이 (요약)

| 항목 | 이전 판 | 본 개정판 |
|---|---|---|
| 저장 단위 | 여행지(Place) — 모두가 공유 | **방문 기록(VisitRecord) — 개인 소유** |
| 같은 장소 | 1건으로 수렴, 여러 사용자가 리뷰 | **매번 별개 기록**, 방문 시점별로 남음 |
| 평가 | 타인이 남기는 리뷰(별점+코멘트) 다수, 평균 별점 집계 | **기록에 흡수된 본인 평점 1개**, 평균 없음 |
| 공개 | 전부 공개, 비로그인도 전체 조회 | **기본 비공개**, PUBLIC만 비로그인 열람 |
| 공유 | 개념 없음 | 그룹(조회 전용 재사용 리스트) 단위 공유 |

폐기된 개념: `Place` 공유 엔티티, `Review` 엔티티와 관련 API, 평균 별점(`rating.average`)과
별점 분포, 리뷰 목록 화면, 타인 리뷰 작성·수정·삭제 권한.

## 2. 사용자 및 권한

### 2.1 로그인
- 로그인 수단은 **네이버 OAuth 2.0 하나뿐**이다. 자체 회원가입/이메일·비밀번호 로그인,
  다른 소셜 로그인은 제공하지 않는다.
- 최초 로그인 계정은 자동 가입(upsert)되며, 이후 `provider("naver") + providerId` 로 매칭한다.
- 로그인 성공 시 서버가 세션 쿠키를 발급하고, 브라우저는 이후 모든 API 요청에 쿠키를 포함한다.
  로그인 상태는 새로고침 후에도 유지되어야 한다.
- 세션 만료·로그인 실패 시 사용자에게 사유를 안내하고 재로그인을 유도한다.

### 2.2 권한 정책

"열람 대상"은 기록의 공개 범위에 따라 결정되는 상대다 — `PUBLIC` 이면 모두, `GROUP` 이면
공유된 그룹의 멤버, `PRIVATE` 이면 아무도 아니다.

| 행위 | 비로그인 | 로그인 사용자 | 열람 대상 | 작성자 본인 |
|---|:---:|:---:|:---:|:---:|
| `PUBLIC` 기록 조회 | ✅ | ✅ | ✅ | ✅ |
| `GROUP` 기록 조회 | ❌ | ❌ | ✅ | ✅ |
| `PRIVATE` 기록 조회 | ❌ | ❌ | — | ✅ |
| 기록 등록 | ❌ | ✅ | - | - |
| 기록 수정·삭제 | ❌ | ❌ | ❌ | ✅ |
| 공개 범위·공유 그룹 변경 | ❌ | ❌ | ❌ | ✅ |
| 사진 업로드·삭제 | ❌ | ❌ | ❌ | ✅ |
| 장소 검색 (등록용) | ✅ | ✅ | ✅ | ✅ |
| 그룹 생성 | ❌ | ✅ | - | - |
| 그룹 초대 링크 발급·멤버 제외·그룹 삭제 | ❌ | ❌ | ❌ | ✅ 그룹 소유자 |
| 그룹 탈퇴 | ❌ | ❌ | ✅ 본인 | - |

- **편집 권한은 오직 작성자에게만 있다.** 그룹 멤버라는 사실은 조회 권한만 준다.
  공동 편집은 본 버전 범위 밖이다 (§8).
- 열람 권한이 없는 기록에 접근하면 `403` 이 아니라 **`404` 로 응답한다.** `403` 은 "그 기록이
  존재한다"는 사실을 알려주는 셈이라, 비공개 기록의 존재 자체를 숨기기 위해서다.
- 인증이 필요한 동작을 비로그인 상태로 시도하면 `401` 이며, 화면은 로그인으로 유도한다.
- 인가 판정의 최종 책임은 백엔드에 있다. 프론트엔드의 버튼 노출 제어는 사용자 편의일 뿐
  보안 경계가 아니다.

## 3. 도메인 규칙

### 3.1 방문 기록
- 방문 기록은 **개인 데이터**다. 소유·편집 주체는 작성자 한 명이며 이는 공개 범위와 무관하다.
- 같은 장소를 다시 방문하면 **매번 새 기록**을 만든다. 방문 시점마다 사진·메모·평점이 다르기
  때문이다. 같은 사용자가 같은 장소에 여러 기록을 갖는 것도, 서로 다른 사용자가 같은 장소에
  각자의 기록을 갖는 것도 정상이며 중복으로 취급하지 않는다.
- 기록은 장소 정보(장소명, 주소, 좌표, 원본 링크)를 **스냅샷으로 복사해 보관한다** (§3.5).
- 하나의 기록은 카테고리 1개, 태그 0개 이상, 사진 0장 이상, 평점 1개, 메모 0~1개를 가진다.

### 3.2 공개 범위
- 공개 범위는 기록마다 따로 정하며 값은 셋 중 하나다.

| 값 | 의미 |
|---|---|
| `PRIVATE` | 작성자 본인만 볼 수 있다. **기본값** |
| `GROUP` | 작성자가 지정한 그룹(들)의 멤버가 볼 수 있다 |
| `PUBLIC` | 로그인 여부와 무관하게 누구나 볼 수 있다 |

- **기본값은 `PRIVATE` 다.** 등록 폼에서 공개 범위를 선택할 수 있으나 최초 선택 상태는 항상
  `PRIVATE` 이며, 사용자가 명시적으로 넓히지 않는 한 공개되지 않는다.
- 공개 범위는 등록 이후 상세 화면에서 언제든 바꿀 수 있다. 좁히는 변경은 즉시 적용되어
  이전에 볼 수 있던 사용자도 더 이상 볼 수 없게 된다.
- `GROUP` 인 기록은 **여러 그룹에 동시에 공유할 수 있다.** (가족 여행을 "가족"과 "친구" 양쪽에
  공유하는 상황이 자연스럽게 처리된다.)
- `GROUP` 을 선택했는데 공유 그룹이 하나도 지정되지 않은 상태는 **`PRIVATE` 과 동일하게 취급한다**
  (작성자 외에는 아무도 볼 수 없다). 저장은 허용하되 화면에서 이 사실을 안내한다.
- 공개 범위와 공유 그룹 목록은 **작성자에게만 보인다.** 열람자에게는 노출하지 않는다.

### 3.3 그룹
- 그룹은 **조회 전용 공유 대상 목록**이다. Instagram 친한 친구 리스트와 같은 성격으로,
  한 번 만든 그룹을 여러 기록에 반복 재사용한다.
- **그룹은 권한 범위가 아니다.** 멤버는 그 그룹으로 공유 설정된 기록만 볼 수 있으며,
  그룹 소유자나 다른 멤버의 나머지 기록에는 접근할 수 없다.
- 그룹 소유자만 초대 링크 발급, 멤버 제외, 그룹 삭제를 할 수 있다. 소유자는 그룹의 멤버로도
  포함되며 탈퇴할 수 없다 (그룹을 없애려면 삭제한다).
- 멤버는 언제든 스스로 탈퇴할 수 있다. 탈퇴하면 해당 그룹으로 공유된 기록이 즉시 보이지 않는다.
- **그룹당 최대 5명**(소유자 포함)이다. 소규모 동행·가족 단위 공유를 전제로 한 값이며,
  정원이 찬 그룹의 초대 수락은 거부된다.
- 그룹 삭제 시 그 그룹으로만 공유되던 기록은 실질적으로 비공개가 된다. 기록 자체는 삭제되지 않는다.

### 3.4 초대
- 멤버 추가는 **초대 링크**로만 한다. 가입자 검색 기능은 제공하지 않는다 — 이름·이메일로
  전체 가입자를 검색하게 만들면 개인정보가 노출되기 때문이다.
- 초대 링크는 그룹 소유자가 발급하며, 링크를 받은 사람이 **로그인한 뒤 수락**해야 멤버가 된다.
- 초대 링크의 **유효 기간은 발급 후 7일**이다. 만료된 링크로 수락을 시도하면 거부되고
  사유를 안내한다.
- **그룹당 유효한 초대 링크는 1개만 존재한다.** 재발급하면 이전 링크는 즉시 무효가 된다.
  메신저 대화방에 남은 오래된 링크가 계속 유효한 상태를 막기 위해서다.
- 링크 하나로 여러 명이 수락할 수 있으며, 정원(5명)에 도달하면 더 이상 수락되지 않는다.
- 이미 멤버인 사용자가 다시 수락하면 오류 없이 그대로 멤버 상태를 유지한다.

### 3.5 장소 데이터
- 장소 마스터 데이터는 **외부 검색 API로 조회할 뿐 자체 저장 단위로 두지 않는다.**
  장소 테이블도, 장소 단위의 집계도 없다.
- 외부 장소 검색은 안정적인 고유 식별자를 제공하지 않으므로 (§4.2), 기록은 **외부 장소 ID를
  보관하지 않는다.** 대신 검색 결과의 장소명·주소·좌표·원본 링크를 기록에 복사해 저장한다.
- 이 값들은 등록 시점의 **스냅샷**이다. 외부에서 장소 정보가 바뀌어도 이미 만들어진 기록은
  갱신되지 않는다. 기록은 "그때 내가 방문한 곳"을 남기는 것이므로 이 동작이 의도된 것이다.
- 중복 판정 규칙은 두지 않는다. 같은 장소의 기록이 여러 건 존재하는 것이 정상이기 때문이다 (§3.1).

### 3.6 평점과 메모
- 평점과 메모는 **기록 자체의 필드**다. 별도 엔티티나 별도 API로 분리하지 않는다.
- 평점은 기록당 정확히 1개이며 **필수**다. 작성자 본인의 평가이므로 타인이 남기거나 고칠 수 없다.
- 평점은 **0.5 ~ 5.0 범위의 0.5 단위 값**만 허용한다. 화면 입력(별 반 개 단위)과 서버
  검증·저장 규칙이 동일하며, `3.7` 같은 값은 검증 실패다.
- **평균 별점·별점 분포·리뷰 수 개념은 존재하지 않는다.** 기록마다 평점이 하나뿐이므로
  집계 대상이 없다. 화면은 기록의 평점을 그대로 ★ N.N 으로 표시한다.
- 메모는 선택 입력이며 1000자 이하다. 메모 없이 평점만 남기는 것을 허용한다.
- 수정 여부는 생성 시각과 수정 시각의 비교로 판단한다. 같으면 미수정, 다르면 "수정됨"이다.

### 3.7 삭제 정책
- 방문 기록은 **물리 삭제하지 않고 삭제 시각을 기록한다**(soft delete). 통계·이력 보존과
  오삭제 복구를 위해서다. 삭제된 기록은 조회에서 제외되며 `404` 로 응답된다.
- 별도의 삭제 여부 플래그 컬럼은 두지 않는다. 같은 사실을 두 곳에 적으면 어긋날 수 있고,
  삭제 시각 하나로 "삭제 여부"와 "삭제 시각"을 모두 답할 수 있다.
- 사진은 파일 본체를 함께 지워야 하므로 soft delete 대상이 아니며 물리 삭제한다.
- 그룹, 그룹 멤버, 공유 관계, 초대는 soft delete 대상이 아니다. 공유 해제와 그룹 탈퇴는
  즉시 조회 권한을 없애야 하므로 흔적을 남기지 않는다.

### 3.8 좌표
위치는 WGS84 위경도로 다루며 지도 표시에 그대로 쓸 수 있는 정밀도(소수점 6자리 이상)를 유지한다.
외부 검색 결과의 좌표는 **백엔드가 변환한 뒤** 프론트엔드로 내려준다 (§4.2).

## 4. 외부 연동

### 4.1 사용하는 네이버 제품

| 제품 | 발급처 | 용도 | 키 보관 |
|---|---|---|---|
| 네이버 로그인 (OAuth 2.0) | developers.naver.com | 로그인·회원 식별 | 백엔드 전용 |
| 네이버 검색 오픈API – 지역(Local) | developers.naver.com | 등록용 장소 검색의 실제 구현체 | **백엔드 전용** (브라우저 노출 금지) |
| NCP Maps (JS SDK, Geocoder) | ncloud.com | 지도 표시·주소↔좌표 변환 | 프론트엔드 Client ID |

세 가지는 **서로 다른 애플리케이션 등록과 키 발급**이 각각 선행되어야 한다.
키워드 기반 장소 검색은 NCP Maps 단독으로 제공되지 않으므로 지역 검색 오픈API가 반드시 필요하다.

### 4.2 지역 검색 오픈API의 구조적 제약 (설계 전제)
아래는 기술 검증으로 확인된 사실이며, 양쪽 모듈의 설계 근거가 된다.

1. **검색어당 최대 5건**만 반환한다 (`display` 최대 5, `start` 사실상 1).
2. **좌표/반경 기반 검색이나 거리순 정렬 파라미터를 지원하지 않는다.**
3. **브라우저에서 직접 호출할 수 없다** (CORS 미지원, 키 노출 문제) → 백엔드 프록시 필수.
4. 응답 좌표(`mapx`, `mapy`)는 WGS84가 아닌 정수형(10^7 배율) 좌표다 → 백엔드가 변환한다.
5. **고유 장소 ID 필드가 없다.** 자체 식별자(해시/UUID) 부여도 검토했으나, 같은 장소가 검색어에 따라
   조금씩 다른 이름·주소로 내려와 안정적 매칭 근거가 되지 못해 **기각되었다.** 대신 원본 링크를
   참고값으로 기록에 보관한다 (§3.5).

**따라서 "장소 검색 결과 10건씩 페이지네이션 + 기준 위치 거리순 정렬" 요구사항은
백엔드 집계로만 충족 가능하다.** 백엔드가 검색어 변형으로 여러 번 호출해 후보를 모으고,
중복 제거 → 좌표 변환 → 거리 계산 → 정렬 → 페이지네이션한 결과를 내려준다.
그럼에도 원본 API의 5건 상한 때문에 후보 풀이 10건에 못 미칠 수 있으며,
이 경우 확보된 만큼만 반환한다.

### 4.3 지도 표시
- 지도 표시는 **프론트엔드가 NCP Maps JS SDK로 직접 렌더링**하며 백엔드는 개입하지 않는다.
- Client ID가 설정되지 않은 환경에서는 새 창으로 네이버 지도 검색 결과를 여는 방식으로 대체한다.
- 좌표↔주소 변환이 서버 측에서 필요해지면 백엔드가 Geocoder를 보조적으로 사용할 수 있다.

## 5. 기준 위치와 위치정보 정책

거리순 정렬(목록·장소 검색)의 기준이 되는 사용자 위치는 다음 우선순위로 결정한다.

1. 브라우저 Geolocation API로 현재 위치 획득 (HTTPS 또는 localhost, 사용자 권한 허용 필요)
2. 사용자가 직접 지정한 기준 위치 (지도 클릭 또는 장소 검색으로 선택)
3. 위 둘 다 불가한 경우 **기본 위치는 서울역** (위도 37.5546, 경도 126.9706)

**위치정보법 원칙** — 이용자의 실시간 위치정보를 수집·저장하는 사업은 위치기반서비스사업자에
해당되어 신고 의무 등이 발생할 수 있다. 이를 회피하기 위해 다음을 원칙으로 한다.

- Geolocation으로 얻은 좌표는 **클라이언트에서만, 일시적으로** 사용한다.
- 사용자의 실제 GPS 좌표를 **계정·사용자 식별자와 연결해 저장하지 않는다.** 거리 계산을 위해
  서버로 전달되는 좌표는 요청 파라미터로만 쓰이고 사용자와 묶어 영속화하지 않는다.
- 사용자가 지정한 기준 위치는 브라우저 로컬 저장소에만 보관한다.
- 방문 기록에 저장되는 "장소"의 위치는 장소 정보이지 개인 위치정보가 아니므로 별개다.

실제 신고 대상 해당 여부는 법률 자문으로 별도 확인이 필요하다 (§8).

## 6. 모듈 간 연동 계약

### 6.1 통신 방식
- 기본 경로 `/api`, 요청·응답은 JSON (사진 업로드만 `multipart/form-data`).
- 인증은 **세션 쿠키** 기반이다. 프론트엔드는 모든 API 요청에 자격 증명을 포함해 보낸다.
- 세션 쿠키는 `HttpOnly` 이며 운영 환경에서는 `Secure` 를 적용한다.
- 세션 쿠키 인증이므로 **CSRF 보호를 유지한다.** 서버가 CSRF 토큰 쿠키를 내려주고,
  프론트엔드는 쓰기 요청(`POST`/`PUT`/`PATCH`/`DELETE`) 시 해당 값을 요청 헤더로 함께 보낸다.
  헤더가 없으면 `403` 이다.
- CORS: 프론트엔드 개발 서버 오리진(기본 `http://localhost:5173`)을 허용하고 자격 증명 포함 요청을 허용한다.

### 6.2 목록의 세 가지 범위
목록 조회는 **어떤 범위의 기록을 보는지**를 항상 명시한다. 세 범위는 성격이 달라 한 목록에
섞지 않는다.

| 범위 | 내용 | 비로그인 |
|---|---|:---:|
| 내 기록 | 작성자가 본인인 기록 전부 (공개 범위 무관) | ❌ |
| 공유받은 기록 | 내가 속한 그룹으로 공유된 **타인의** 기록 | ❌ |
| 둘러보기 | `PUBLIC` 기록 전부 (본인 것 포함) | ✅ |

- 프론트엔드는 이 범위를 탭으로 제공하고, 백엔드는 목록 조회 시 범위를 필수 파라미터로 받는다.
- 어떤 범위로 조회하든 서버는 요청자가 볼 권한이 있는 기록만 반환한다. 범위 파라미터는
  권한을 넓히는 수단이 아니라 이미 볼 수 있는 것 중에서 고르는 수단이다.
- 카테고리 필터와 정렬은 세 범위 모두에 동일하게 적용된다.

### 6.3 페이지네이션
목록 응답은 `content`, `page`(0-base), `size`, `totalElements`, `totalPages` 를 포함한다.
기록 목록과 장소 검색 결과는 **페이지당 10건**이 기본이다.

### 6.4 오류 응답
모든 실패 응답은 다음 스키마로 통일한다. 프론트엔드는 HTTP 상태와 무관하게 `code` 로 분기할 수 있다.

```json
{ "code": "RECORD_NOT_FOUND", "message": "존재하지 않는 기록입니다.", "status": 404 }
```

| 상황 | 상태 | code |
|---|---|---|
| 입력값 검증 실패 (필수값 누락, 카테고리·평점 범위/단위 오류, 파싱 실패) | 400 | `VALIDATION_ERROR` |
| 허용되지 않는 파일 형식·용량 초과 | 400 | `INVALID_FILE` |
| 비로그인 상태의 보호된 API 접근 | 401 | `UNAUTHENTICATED` |
| 권한 없는 수정·삭제 시도 | 403 | `FORBIDDEN` |
| 존재하지 않거나 **열람 권한이 없는** 기록 | 404 | `RECORD_NOT_FOUND` |
| 존재하지 않는 그룹·사진 | 404 | `GROUP_NOT_FOUND` / `PHOTO_NOT_FOUND` |
| 유효하지 않은 초대 링크 | 404 | `INVITE_NOT_FOUND` |
| 만료된 초대 링크 | 410 | `INVITE_EXPIRED` |
| 그룹 정원(5명) 초과 | 409 | `GROUP_MEMBER_LIMIT_EXCEEDED` |
| 지원하지 않는 메서드 / `Content-Type` | 405 / 415 | `METHOD_NOT_ALLOWED` / `UNSUPPORTED_MEDIA_TYPE` |
| 그 밖의 DB 제약 위반 (동시 요청 경합 등) | 409 | `CONFLICT` |
| 매핑되지 않은 주소, 존재하지 않는 정적 파일 | 404 | `NOT_FOUND` |
| 지역 검색 오픈API 호출 실패·한도 초과 | 502 | `PLACE_SEARCH_UNAVAILABLE` |
| 그 밖의 처리되지 않은 예외 | 500 | `INTERNAL_ERROR` |

- **열람 권한이 없는 기록은 `403` 이 아니라 `404`(`RECORD_NOT_FOUND`)다** (§2.2). 존재 여부를
  드러내지 않기 위해서이며, 응답 본문도 실제로 없는 기록과 구분되지 않아야 한다.
- `400` 의 `message` 는 어느 필드가 왜 틀렸는지 담는다.
- `409`·`500` 의 `message` 는 고정 문구다. 내부 구조(테이블·제약 이름)가 드러나지 않도록
  예외 메시지를 응답에 싣지 않고 서버 로그로만 남긴다.

### 6.5 기록 등록 플로우
**등록 화면 한 번의 제출이 백엔드 API 2회 호출로 나뉜다.** 한 트랜잭션으로 묶는 API는 없다.

| 순서 | 호출 | 폼에서 오는 값 |
|---|---|---|
| 1 | 기록 생성 | 장소명, 카테고리, 위치(주소·좌표), 원본 링크, 태그, 메모, **평점**, **공개 범위·공유 그룹** |
| 2 | 사진 업로드 | 사진 (1의 응답 식별자 필요, multipart) |

- 평점이 기록의 필드가 되면서 이전 판의 3단계(기록 생성 → 사진 → 별점)가 2단계로 줄었다.
- 1이 성공하고 2가 실패하면 **사진이 빠진 기록이 남는다.** 프론트엔드가 사진 업로드를
  재시도하거나 사용자에게 안내해야 한다. 기록 자체는 유효하므로 되돌리지 않는다.
- 사진이 첨부되지 않은 등록은 1회 호출로 끝난다.

### 6.6 장소 검색 → 등록 폼 전달값
장소명은 **직접 입력할 수 없고** 검색 결과 선택으로만 채워진다. 선택 시 다음 값이 함께 전달되며,
화면에 노출되지 않더라도 등록 요청에 포함되어야 한다.

- 장소명, 주소·도로명주소, 좌표(WGS84), 원본 링크
- 장소 ID는 존재하지 않으므로 보관·전송하지 않는다 (§3.5, §4.2)

### 6.7 작성자 정보 노출 범위
- 기록 응답에 포함되는 작성자 정보는 **이름과 프로필 사진**까지다. 공개 범위와 무관하게
  동일하며, `PUBLIC` 기록도 예외가 아니다.
- **이메일은 어떤 응답에도 포함하지 않는다.** 로그인 사용자 본인 정보 조회는 예외다.
- 그룹 멤버 목록에는 멤버의 이름과 프로필 사진만 노출한다.

### 6.8 사진 업로드 제한 (양쪽 동일 값 유지)

| 항목 | 값 |
|---|---|
| 파일당 최대 크기 | 5MB |
| 한 번에 첨부하는 전체 합계 | 30MB |
| 허용 포맷 | `image/jpeg`, `image/png`, `image/webp` |

- 프론트엔드·백엔드 모두 설정값으로 외부화하며 **같은 값을 유지해야 한다.**
- 클라이언트 검증은 사용자 편의용이고, **최종 검증 책임은 백엔드에 있다.**
- 사진 URL은 저장 방식(파일시스템/오브젝트 스토리지)과 무관하게 항상 접근 가능한 URL로 응답하므로,
  저장소가 전환되어도 프론트엔드 동작은 동일하다.
- **비공개 기록의 사진 URL은 그 자체로 접근 가능하다.** 파일 경로를 아는 사람은 기록의 공개 범위와
  무관하게 이미지를 볼 수 있다는 뜻이며, 현재 단계에서는 추측 불가능한 UUID 경로에 의존한다.
  공개 범위를 파일 서빙까지 강제하는 것은 후속 과제다 (§8).

## 7. 공통 비기능 요구사항
- 조회는 페이지네이션을 기본으로 하고, 거리순 정렬 계산은 서버가 수행한다.
- 모든 쓰기 요청은 서버에서 입력값 검증을 수행한다.
- 외부 API 키는 노출 범위에 맞게 분리 보관한다 (§4.1). 지역 검색·OAuth 키는 백엔드 환경변수로만
  관리하고 브라우저 번들에 포함하지 않는다.
- 로딩·오류 상태를 사용자에게 드러낸다 (목록 조회, 등록, 사진 업로드, 지도 로딩, 초대 수락).
- 반응형 레이아웃으로 모바일(360px~)부터 데스크톱까지 대응한다.
- 공개 범위가 걸린 화면 요소(공개 범위 배지, 공유 그룹 목록)는 오해가 생기지 않도록
  현재 상태를 항상 명시적으로 표시한다.

## 8. 공통 제약사항 및 향후 과제
- **보안 임시 비활성화**: 백엔드 `SecurityConfig` 가 개발 편의를 위해 모든 요청을 열어 두고
  있고, 인증은 컨트롤러가 직접 막고 있다. 공개 범위 판정 자체는 서비스 계층에 구현되어 있지만,
  인가의 첫 관문을 필터체인으로 되돌리는 작업이 남아 있다.
- **프론트엔드가 아직 API를 호출하지 않는다.** 현재는 목업 데이터로 화면을 구성하며, 연동 시
  응답 스키마에 맞춘 매핑 레이어가 필요하다.
- **공동 편집은 범위 밖이다.** REFERENCE.md의 미결 사항은 "조회 전용 재사용 리스트"로 결론냈다.
  공동 편집(멤버가 같은 기록을 수정)이 필요해지면 편집 충돌 처리·이력 관리가 추가로 필요하므로
  별도 설계가 선행되어야 한다.
- **공개 링크 공유는 지원하지 않는다.** 비공개 기록을 토큰 URL로 열람시키는 방식(Wandrly 류)은
  검토했으나 이번 버전에서 제외했다. 필요해지면 그룹과 별개 개념으로 추가한다.
- **사진 파일 서빙에 공개 범위가 적용되지 않는다** (§6.8). 서명 URL 또는 인가 기반 서빙으로의
  전환이 후속 과제다.
- **장소 검색 후보 풀 부족 가능성**: 원본 API의 검색어당 5건 상한 때문에 백엔드 집계로도
  "10건 페이지네이션 + 거리순 정렬" 요구 수준을 못 채울 수 있다. 구현 착수 전 재검토하고,
  필요 시 다른 POI 데이터 소스(예: 카카오 로컬 API) 병행을 검토한다.
- **태그 입력·필터 UI 미구현**: 백엔드는 태그 저장과 자동완성을 지원하지만 등록 폼·목록 필터에
  태그 UI가 없어 등록 요청이 항상 빈 태그 목록으로 나간다.
- **스키마 마이그레이션 도구 부재**: 운영 데이터가 생기기 전에 Flyway/Liquibase 도입이 선행되어야 한다.
  본 개정은 테이블 구조를 크게 바꾸므로 더욱 그렇다.
- **외부 API 재검증 항목**: 지역 검색의 정확한 좌표계와 일일 호출 한도는 구현 착수 시점에
  공식 문서로 다시 확인한다.
- **위치정보법 해당 여부**는 §5 원칙을 지키되 법률 자문으로 별도 확인이 필요하다.
- **사진 저장소 전환**(파일시스템 → 오브젝트 스토리지)과 기존 데이터 마이그레이션 절차는 후속 명세에서 다룬다.
