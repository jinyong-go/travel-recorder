# 여행 지도 (travel-recorder) 공통 요구사항 명세서

다녀온 **여행**을 단위로 방문한 장소를 기록하고, 원하는 상대에게만 공유하는 웹 애플리케이션.

## 0. 이 문서의 위치

본 문서는 **서비스가 무엇인지**를 정의한다. 도메인 개념, 각 개념이 지켜야 하는 규칙,
사용자가 하는 핵심 행위, 외부 서비스가 강제하는 제약이 여기에 속한다.

**구현 수단은 여기에 쓰지 않는다.** 엔드포인트 시그니처·엔티티 컬럼·오류 코드 표·쿼리
파라미터·화면 구조·컴포넌트·라우트는 전부 모듈 명세의 몫이다. 같은 내용을 두 곳에 적으면
한쪽만 고쳐졌을 때 어느 쪽이 맞는지 알 수 없다. **이 원칙은 본 문서 안에서도 적용된다** —
§2는 행위의 흐름과 진입 조건만, §3은 개념의 규칙만 말하고 서로 반복하지 않는다.

| 문서 | 다루는 범위 |
|---|---|
| 본 문서 (`SPECIFICATION.md`) | 서비스 정의, 도메인 개념과 규칙, 핵심 행위, 공개 범위 정책, 외부 API 제약, 모듈 간 계약 |
| [`backend/SPECIFICATION.md`](./backend/SPECIFICATION.md) | 엔티티·테이블, 인가 구현, REST API 명세(경로·파라미터·응답), 오류 코드, 페이지네이션 규격, 사진 저장소 설계 |
| [`frontend/SPECIFICATION.md`](./frontend/SPECIFICATION.md) | 화면·라우트, 탭·폼·모달 동작, 문구, 테마, 접근성, 클라이언트 검증 |

**충돌 시 본 문서가 우선한다.** 모듈 명세는 본 문서의 규칙을 구체화할 수는 있어도 뒤집을 수 없다.

> **이 개정판은 [설계 검토 문서](./REFERENCE.md)의 결론을 반영한다.** 두 차례의 모델 전환이 누적되어 있다.
>
> 1. "모두가 공유하는 여행지 + 여러 사용자의 리뷰" → **개인 소유의 방문 기록 + 공개 범위** (검토 1·2)
> 2. 평면적인 방문 기록 목록 → **여행(Trip)을 상위 그룹으로 두는 2계층 구조**, 공유의 단위도 여행 (검토 3)
>
> 1은 코드에 반영되어 있고, **2는 명세만 개정된 상태로 구현이 아직 따라오지 않았다.** 남은 차이는 §7에 정리한다.

## 1. 서비스 개요

### 1.1 목적과 전제
네이버 계정으로 로그인한 사용자가 여행을 만들고 그 아래에 방문 장소를 등록하며, 각 장소에
사진·메모·평점을 남긴다. 서비스 전체가 아래 세 가지 전제 위에 서 있다.

| 전제 | 뜻 |
|---|---|
| **여행이 단위다** | 기록은 여행 없이 존재할 수 없고, 공유도 여행 단위로만 일어난다 |
| **기본은 비공개다** | 사용자가 명시적으로 넓히지 않는 한 아무에게도 보이지 않는다 |
| **편집자는 소유자 한 명이다** | 공유는 보여주는 것이지 함께 쓰는 것이 아니다 |

이 셋과 어긋나는 요구가 생기면 기능을 덧붙이기 전에 전제부터 다시 검토해야 한다.

### 1.2 구성
프론트엔드와 백엔드가 분리된 2모듈 구조이며, 브라우저 ↔ 백엔드는 JSON REST API + 세션 쿠키로
통신한다 (§6). 각 모듈의 기술 스택은 [README](./README.md)를 참고한다.

## 2. 핵심 행위

사용자가 이 서비스에서 하는 일은 다섯 가지다. **여기서는 순서·진입 조건·결과만 다룬다.**
각 개념의 값 규칙(무엇이 유효한 입력인지, 무엇이 필수인지)은 §3에 있고 여기서 반복하지 않는다.

```
로그인 ──▶ 여행 생성 ──▶ 여행지 등록 ──▶ (사진 업로드)
             │
             └──▶ 공유 ◀── 그룹 생성·멤버 추가
```

### 2.1 로그인
- 모든 쓰기 행위의 전제다. **비로그인 상태로 할 수 있는 일은 `PUBLIC` 여행 열람과 장소 검색
  두 가지뿐이며**, 그 외에는 전부 로그인을 요구한다.
- 가입 절차가 따로 없다. 처음 로그인하면 그 자리에서 계정이 만들어진다 (§3.1).
- 로그인 상태는 새로고침 후에도 유지된다. 세션이 끊기면 사유를 알리고 재로그인으로 유도한다.
- **예외는 없다.** 초대도 로그인해야 확인할 수 있다 — 초대는 받은 사람의 계정에 쌓이므로
  로그인 전에는 보여줄 대상 자체가 없다 (§3.7).
- 로그인이 필요한 곳에 비로그인으로 닿으면 **"권한 없음"이 아니라 "로그인이 필요하다"고 답한다.**
  §2.6의 존재 은닉과는 다른 상황이다 — 숨겨야 할 대상이 아직 특정되지 않았고, 사용자가 할 일이
  로그인 하나로 분명하기 때문이다. 은닉이 필요한 판정은 로그인한 뒤에 시작된다.

### 2.2 여행 생성
- 로그인한 사용자는 누구나 할 수 있다. **만든 사람이 소유자가 되고 소유자는 바뀌지 않는다.**
- 이 시점에 **공개 범위가 정해진다.** 이후 등록되는 하위 기록은 전부 이 범위를 따르므로,
  여행 생성은 곧 "이 여행을 누구까지 보게 할지"를 정하는 행위다 (§3.5).
- **대표 사진은 여기서 정할 수 없다.** 하위 기록의 사진 중에서 고르는 값이라 §2.4 이후에야
  지정 대상이 생긴다 (§3.2).
- 결과로 **기록이 0건인 여행**이 남는다. 이는 정상 상태이며, 여행을 먼저 만들고 장소를 채워
  넣는 것이 의도된 동선이다.

### 2.3 여행지 등록
여행 하위에 기록(TripRecord)을 한 건 만드는 행위다.

- **소속 여행을 먼저 정해야 시작할 수 있다.** 여행 없는 기록은 존재할 수 없다 (§3.3).
- **그 여행의 소유자만 할 수 있다.** 남의 여행에 기록을 더하는 경로는 없다.
- 장소는 **외부 검색 결과에서 고르는 것으로만** 채워진다. 직접 입력하는 경로가 없으므로
  장소 검색이 이 행위의 선행 단계다 (§3.8).
- **공개 범위를 입력받지 않는다.** 소속 여행에서 이미 정해졌다 (§3.5).
- 사진을 첨부했다면 기록 생성 뒤 별도 단계로 업로드된다. 중간에 실패했을 때 무엇이 남는지는
  §6.2에 있다.

### 2.4 그룹 생성과 멤버 추가
- 로그인한 사용자는 누구나 그룹을 만들 수 있다. **만든 사람이 그룹 소유자이며 동시에 멤버다.**
- **멤버는 초대를 통해서만 늘어난다.** 소유자가 상대의 이메일로 초대를 보내고, 받은 사람이
  자기 초대 목록에서 수락해야 멤버가 된다. 받은 사람은 거절할 수도 있다 (§3.7).
- 그룹을 만드는 것만으로는 아무것도 공유되지 않는다. **공유는 여행 쪽에서 그룹을 지정해야
  일어난다** (§2.5).
- 멤버 자격은 **조회 권한만** 준다. 그룹에 속했다고 남의 기록을 고칠 수 있게 되지 않는다 (§3.6).

### 2.5 공유
- 공유의 단위는 **여행**이다. 여행 하나를 공유하면 **그 하위 기록이 전부 함께** 보인다.
  기록 하나만 따로 공유하거나 따로 감추는 수단은 없다 (§3.5).
- 공유는 별도의 행위가 아니라 **여행의 공개 범위를 바꾸는 일**이다. 언제든 다시 바꿀 수 있고,
  좁히는 변경은 즉시 적용되어 직전까지 보던 사람도 그 순간부터 볼 수 없다.
- 공유는 **편집 권한을 주지 않는다.** 열람자는 보기만 한다.
- 누구에게 공유했는지는 **소유자만 안다** (§3.5).

### 2.6 행위별 권한 요약

행의 "소유자"는 그 행위의 대상을 소유한 사람이다 — 여행·기록·사진은 여행 소유자,
그룹·초대는 그룹 소유자를 뜻한다.

| 행위 | 비로그인 | 로그인 사용자 | 열람 대상 | 소유자 |
|---|:---:|:---:|:---:|:---:|
| `PUBLIC` 여행·하위 기록 조회 | ✅ | ✅ | ✅ | ✅ |
| `GROUP` 여행·하위 기록 조회 | ❌ | ❌ | ✅ | ✅ |
| `PRIVATE` 여행·하위 기록 조회 | ❌ | ❌ | — | ✅ |
| 장소 검색 | ✅ | ✅ | ✅ | ✅ |
| 여행 생성 · 그룹 생성 | ❌ | ✅ | - | - |
| 여행 수정·삭제 | ❌ | ❌ | ❌ | ✅ |
| 공개 범위·공유 그룹 변경 | ❌ | ❌ | ❌ | ✅ |
| 기록 등록·수정·삭제·여행 간 이동 | ❌ | ❌ | ❌ | ✅ |
| 사진 업로드·삭제, 대표 사진 지정 | ❌ | ❌ | ❌ | ✅ |
| 초대 보내기·철회·멤버 제외·그룹 삭제 | ❌ | ❌ | ❌ | ✅ |
| 받은 초대 조회·수락·거절 | ❌ | ✅ 본인 것만 | - | - |
| 그룹 탈퇴 | ❌ | ❌ | ✅ 본인 | ❌ 소유자는 불가 |

- "열람 대상"은 여행의 공개 범위에 따라 결정되는 상대다 — `PUBLIC` 이면 모두, `GROUP` 이면
  공유된 그룹의 멤버, `PRIVATE` 이면 아무도 아니다 (§3.5).
- 열람 권한이 없는 대상에 접근하면 **"권한 없음"이 아니라 "존재하지 않음"으로 응답한다.**
  "권한이 없다"는 답 자체가 그것이 존재한다는 사실을 알려주기 때문이며, 응답은 실제로 없는
  경우와 구분되지 않아야 한다.
- **초대도 같은 규칙을 따른다.** 소유자는 자기가 보낸 초대를, 받은 사람은 자기 앞으로 온 초대를
  본다. 그 밖의 초대에 접근하면 존재하지 않는 것으로 응답한다 (§3.7).
- 인가 판정의 최종 책임은 백엔드에 있다. 프론트엔드의 버튼 노출 제어는 사용자 편의일 뿐
  보안 경계가 아니다.

## 3. 도메인

서비스가 다루는 개념과 각 개념이 지켜야 하는 규칙이다. 괄호 안은 코드·API에서 쓰는 이름이다.
절 순서는 소유 관계를 따른다 — 사용자가 여행을 갖고, 여행이 기록을, 기록이 사진을 갖는다.

| 개념 | 한 줄 정의 | 상세 |
|---|---|---|
| 사용자 (User) | 네이버 계정으로 가입한 사람. 여행·그룹의 소유 주체 | §3.1 |
| 여행 (Trip) | 한 번의 여행. **기록의 상위 그룹이자 공유의 단위** | §3.2 |
| 여행 기록 (TripRecord) | 여행 중 방문한 장소 1건의 기록. **항상 여행 하나에 속한다**. 카테고리·태그·평점·메모를 갖는다 | §3.3 |
| 사진 (Photo) | 기록에 첨부된 이미지. 기록에 종속된다 | §3.4 |
| 공개 범위 (visibility) | 여행을 누가 볼 수 있는지. `PRIVATE`/`GROUP`/`PUBLIC` | §3.5 |
| 그룹 (Group) | 반복 재사용하는 **조회 전용 공유 대상 목록** | §3.6 |
| 초대 (Invite) | 그룹 소유자가 특정 사용자에게 보내는 가입 요청. **멤버를 추가하는 유일한 수단** | §3.7 |
| 장소 (Place) | 외부 검색으로 조회한 지점 정보. **자체 저장 단위가 아니다** | §3.8 |
| 기준 위치 | 거리순 정렬의 기준이 되는 사용자 위치. **저장하지 않는다** | §5 |

> **"여행"과 "여행 기록"은 다른 것이다.** 여행(Trip)은 기간·인원을 갖는 상위 단위이고,
> 여행 기록(TripRecord)은 그 안의 장소 한 곳이다. 화면에서는 기록을 "여행지"로도 부른다.

### 3.1 사용자
- 계정은 **네이버 OAuth 2.0 로그인 하나로만** 만들어진다. `provider("naver") + providerId` 로
  동일인을 판정하며, 최초 로그인 시 자동 가입된다.
- 사용자가 소유하는 것은 **여행**과 **그룹**이다. 기록·사진은 여행에 딸려 오고, 멤버 자격은
  그룹에 딸려 온다.
- **타인에게 노출되는 사용자 정보는 이름과 프로필 사진까지다.** 공개 범위와 무관하게 동일하며,
  `PUBLIC` 여행의 소유자도, 그룹 멤버 목록도 예외가 아니다.
- **이메일은 어떤 응답에도 포함하지 않는다.** 로그인한 본인의 정보 조회만 예외다.
  항목별 노출 범위는 §3.5의 표에 모아 두었다.
- 사용자 삭제(탈퇴)는 본 버전 범위 밖이다 (§7).

### 3.2 여행
- 여행은 **기록의 상위 그룹이자 공유의 단위**다. 소유자는 한 명이고, 그 여행과 하위 기록 전부의
  작성자다. **소유자만 그 여행에 기록을 추가할 수 있다.**
- 여행의 입력 항목은 다음과 같다.

| 항목 | 필수 | 규칙 |
|---|:---:|---|
| 여행 이름 | 필수 | 50자 이하. 예: "제주 3박 4일" |
| 시작일 | 필수 | 날짜 (시각 없음) |
| 종료일 | 필수 | 시작일 이상. 시작일과 같으면 당일치기 |
| 인원 | 필수 | 1 이상의 정수. **본인을 포함한 사람 수** |
| 예산 | 선택 | 0 이상의 정수, 원 단위. 여행 전체 **총액 하나**다 |
| 여행 설명 | 선택 | 자유 텍스트, 1000자 이하 |
| 대표 사진 | 선택 | 그 여행의 하위 기록에 올라온 사진 중 하나를 지정한다 |
| 공개 범위 | 필수 | 기본값 `PRIVATE` (§3.5) |

- **인원은 숫자일 뿐 계정이 아니다.** "네 명이 갔다"와 "이 여행을 누구에게 공유한다"는 별개
  개념이며, 인원 값은 공개 범위·공유 그룹 판정에 전혀 관여하지 않는다 (§3.6).
- **예산은 총액 하나만 받는다.** 항목별 경비 입력·정산·통화 선택은 다루지 않으며 통화는 원(KRW)
  고정이다. 값이 없는 여행은 예산 정보가 없는 것으로 취급하고 화면에서 생략한다.
  **예산은 그 여행을 볼 수 있는 사람에게 함께 보인다** (§3.5).
- **대표 사진은 새로 업로드하지 않는다.** 하위 기록에 이미 올라온 사진 중에서 고르므로 여행
  전용 업로드 경로가 없다. 지정한 사진이 삭제되거나 그 사진을 가진 기록이 삭제되면 지정은
  자동으로 해제되며, 여행 자체는 영향을 받지 않는다.
- **여행당 기록 수에는 제한을 두지 않는다.** 기록이 0건인 여행도 유효하다.
- 여행 기간은 **미래 날짜도 허용한다.** 여행 중에 그날그날 등록하는 사용이 자연스럽고, 서버가
  "다녀온 뒤"임을 판정할 방법도 없다.
- 기록에는 방문 날짜를 두지 않는다. 따라서 여행 기간은 여행 전체를 설명하는 값이며 하위 기록의
  정렬·묶음에 쓰이지 않는다 (§7).
- 어떤 항목이 열람자에게까지 보이는지는 §3.5의 노출 표를 따른다.

### 3.3 여행 기록
- 여행 기록은 **개인 데이터**다. 소유·편집 주체는 소속 여행의 소유자 한 명이며 이는 공개 범위와
  무관하다.
- 기록은 **반드시 여행 하나에 속한다.** 소속 여행은 등록 시점에 정해지며, 등록 이후 **다른 여행으로
  옮길 수 있다.** 단 옮길 수 있는 대상은 본인 소유의 여행뿐이다. 옮기는 순간 그 기록의 열람
  가능 범위가 새 여행의 공개 범위로 바뀐다는 점을 화면에서 알린다.
- 하나의 기록이 갖는 값은 **장소 정보 · 카테고리 · 태그 · 평점 · 메모 · 사진**이다.
  각각의 규칙은 아래 소제목과 §3.4에 있다.
- 기록은 장소 정보(장소명, 주소, 좌표, 원본 링크)를 **스냅샷으로 복사해 보관한다** (§3.8).
- **기록은 공개 범위를 갖지 않는다.** 소속 여행의 공개 범위가 그대로 적용된다 (§3.5).
- 같은 장소를 다시 방문하면 **매번 새 기록**을 만든다. 방문 시점마다 사진·메모·평점이 다르기
  때문이다. 같은 여행 안에 같은 장소의 기록이 여러 건 있는 것도, 서로 다른 사용자가 같은 장소에
  각자의 기록을 갖는 것도 정상이며 중복으로 취급하지 않는다. **중복 판정 규칙을 두지 않는다.**

**카테고리와 태그**

**카테고리**는 기록마다 **정확히 하나**를 갖는 필수 값이며 3종 중 하나다.

| 표시명 | 코드 |
|---|---|
| 관광지 | `SIGHT` |
| 쇼핑 | `SHOPPING` |
| 맛집 | `FOOD` |

- 요청·응답은 코드값을 쓰고 화면 표기는 프론트엔드가 매핑한다.
- 목록 필터의 "전체"는 **카테고리 미지정**을 뜻하며 별도 코드값이 아니다.
- 카테고리와 별개로 **태그**를 붙일 수 있다. 자유 형식 키워드이며 개수 제한 없이 선택 입력이다.
  태그 자동완성 후보는 **요청자가 볼 수 있는 기록에 쓰인 것**으로 제한한다 — 전체 태그를 그대로
  내려주면 비공개 기록에만 쓰인 태그가 노출된다.

**평점과 메모**

- 평점과 메모는 **기록 자체의 값**이다. 별도 개념으로 분리하지 않는다.
- 평점은 기록당 정확히 1개이며 **필수**다. 작성자 본인의 평가이므로 타인이 남기거나 고칠 수 없다.
- 평점은 **0.5 ~ 5.0 범위의 0.5 단위 값**만 허용한다. 화면 입력(별 반 개 단위)과 서버 검증·저장
  규칙이 동일하며, `3.7` 같은 값은 검증 실패다.
- **평균 평점·평점 분포·리뷰 수 개념은 존재하지 않는다.** 기록마다 평점이 하나뿐이라 집계 대상이
  없다. **여행 단위의 평균 평점도 집계하지 않는다** — 한 여행 안의 관광지와 맛집 평점을 평균 내는
  것은 의미가 없기 때문이다.
- 메모는 선택 입력이며 1000자 이하다. 메모 없이 평점만 남기는 것을 허용한다. 여행의 설명(§3.2)과
  기록의 메모는 서로 다른 값이며, 같은 이름(`memo`)을 각 계층에서 쓴다.
- 수정 여부는 생성 시각과 수정 시각의 비교로 판단한다. 같으면 미수정, 다르면 "수정됨"이다.

### 3.4 사진
- 사진은 **기록에 종속된다.** 기록 없이 존재하는 사진은 없으며, 올리고 지울 수 있는 사람은
  소속 여행의 소유자뿐이다.
- 기록당 장수에 제한을 두지 않는다. 한 번에 올릴 수 있는 크기·포맷 제한은 §6.3에 있다.
- 여행의 대표 사진은 **그 여행의 하위 기록에 올라온 사진 중에서** 고른다 (§3.2).
- ⚠️ 사진 파일은 **공개 범위와 무관하게 URL로 접근 가능하다** (§6.3). 비공개 여행의 사진이라도
  경로를 아는 사람은 볼 수 있다는 뜻이다.

### 3.5 공개 범위
- 공개 범위는 **여행마다** 정하며 값은 셋 중 하나다.

| 값 | 의미 |
|---|---|
| `PRIVATE` | 소유자 본인만 볼 수 있다. **기본값** |
| `GROUP` | 소유자가 지정한 그룹(들)의 멤버가 볼 수 있다 |
| `PUBLIC` | 로그인 여부와 무관하게 누구나 볼 수 있다 |

- **공개 범위는 여행에만 존재한다.** 기록을 볼 수 있는지는 "그 기록이 속한 여행을 볼 수 있는가"
  하나로 결정되며, 기록별 예외는 없다. 여행을 공유하면 **하위 기록 전부가 함께** 보이고,
  범위를 좁히면 **하위 기록 전부가 함께** 가려진다.
- **기본값은 `PRIVATE` 다.** 여행 생성 폼에서 공개 범위를 선택할 수 있으나 최초 선택 상태는 항상
  `PRIVATE` 이며, 사용자가 명시적으로 넓히지 않는 한 공개되지 않는다.
- 공개 범위는 생성 이후 언제든 바꿀 수 있다. 좁히는 변경은 즉시 적용되어 이전에 볼 수 있던
  사용자도 더 이상 볼 수 없게 된다.
- `GROUP` 인 여행은 **여러 그룹에 동시에 공유할 수 있다.** (가족 여행을 "가족"과 "친구" 양쪽에
  공유하는 상황이 자연스럽게 처리된다.)
- `GROUP` 을 선택했는데 공유 그룹이 하나도 지정되지 않은 상태는 **`PRIVATE` 과 동일하게 취급한다**
  (소유자 외에는 아무도 볼 수 없다). 저장은 허용하되 화면에서 이 사실을 안내한다.
- 기록을 다른 여행으로 옮기면 공개 범위도 새 여행의 것으로 바뀐다 (§3.3). 이는 범위를 넓히는
  변경이 될 수 있으므로 화면이 결과를 명시적으로 확인시킨다.

**무엇이 누구에게까지 보이는가**

열람 권한이 있다고 해서 모든 값이 보이는 것은 아니다. 공개 범위 판정을 통과한 뒤에도
다음 항목은 소유자에게만 남는다.

| 항목 | 소유자 | 열람 대상 | 근거 |
|---|:---:|:---:|---|
| 여행 이름·기간·인원 | ✅ | ✅ | 인원은 숫자일 뿐 누가 갔는지를 드러내지 않는다 |
| 여행 설명, 대표 사진 | ✅ | ✅ | 여행을 설명하는 값이다 |
| 기록 전체 (장소·카테고리·태그·평점·메모·사진) | ✅ | ✅ | 공유의 대상 그 자체다 |
| 소유자의 이름·프로필 사진 | ✅ | ✅ | §3.1 |
| **예산** | ✅ | ✅ | 얼마를 썼는지는 여행을 설명하는 값이다. 공유의 목적에 포함된다 |
| **공개 범위·공유 그룹 목록** | ✅ | ❌ | 누구에게 공유했는지는 소유자만 아는 정보다 |
| **이메일** | 본인만 | ❌ | §3.1 |

- **예산은 공개 범위를 그대로 따른다.** `PUBLIC` 여행이면 누구에게나 보인다는 뜻이므로,
  공개 범위를 넓히는 화면에서 예산이 함께 공개된다는 사실을 알린다.

### 3.6 그룹
- 그룹은 **조회 전용 공유 대상 목록**이다. Instagram 친한 친구 리스트와 같은 성격으로,
  한 번 만든 그룹을 여러 여행에 반복 재사용한다.
- **그룹은 권한 범위가 아니다.** 멤버는 그 그룹으로 공유 설정된 여행만 볼 수 있으며,
  그룹 소유자나 다른 멤버의 나머지 여행에는 접근할 수 없다.
- **공유 그룹과 여행 인원은 다른 개념이다.** 인원은 함께 간 사람 수이고, 그룹은 기록을 보여줄
  대상이다. 다섯 명이 간 여행을 아무에게도 공유하지 않을 수 있고, 혼자 간 여행을 그룹에 공유할
  수도 있다.
- 그룹 소유자만 초대 보내기·철회, 멤버 제외, 그룹 삭제를 할 수 있다. 소유자는 그룹의 멤버로도
  포함되며 탈퇴할 수 없다 (그룹을 없애려면 삭제한다).
- 멤버는 언제든 스스로 탈퇴할 수 있다. 탈퇴하면 해당 그룹으로 공유된 여행이 즉시 보이지 않는다.
- **그룹당 최대 5명**(소유자 포함)이다. 소규모 동행·가족 단위 공유를 전제로 한 값이며,
  정원이 찬 그룹의 초대 수락은 거부된다.
- 그룹 삭제 시 그 그룹으로만 공유되던 여행은 실질적으로 비공개가 된다. 여행과 기록 자체는
  삭제되지 않는다.
- 그룹 이름은 30자 이하다.

### 3.7 초대
- 멤버 추가는 **초대**로만 한다. 초대는 그룹 소유자가 특정 사용자 앞으로 보내는 요청이며,
  **서비스 안에 기록으로 남는다.** 링크를 발급해 밖으로 내보내는 방식이 아니다.
- **초대 대상은 이메일 정확 일치로 지정한다.** 가입자 검색 기능은 제공하지 않는다 — 이름·이메일
  일부로 전체 가입자를 훑게 만들면 개인정보가 노출되기 때문이다. 소유자는 상대의 이메일을
  이미 알고 있어야 하며, 부분 일치 검색이나 후보 목록은 없다.
- **가입되지 않은 이메일로는 초대할 수 없고, 그 사실을 소유자에게 알린다.** 오타를 알아차릴
  방법이 이것뿐이기 때문이며, 대신 "그 이메일이 가입되어 있다"는 사실이 드러나는 것을 감수한다
  (§7.2).
- **초대는 받은 사람의 계정에 쌓인다.** 로그인한 사용자는 자기 앞으로 온 초대 목록을 보고
  **수락하거나 거절한다.** 수락하면 그 자리에서 멤버가 되고, 거절하면 초대는 사라진다.
- **보낸 사람은 대기 중인 초대를 철회할 수 있다.** 초대는 수락·거절·철회 셋 중 하나로만 끝나며
  **시간이 지났다고 만료되지 않는다.** 초대가 서비스 밖을 돌아다니지 않으므로, "메신저에 남은
  오래된 링크가 계속 유효한 상태"를 막을 이유가 사라졌다.
- **같은 사람에게 같은 그룹의 초대는 한 건만 존재한다.** 대기 중인 초대가 있는 상대를 다시
  초대해도 초대가 늘어나지 않는다.
- **이미 멤버인 사람은 초대할 수 없다.** 반대로 거절한 사람은 다시 초대할 수 있다 — 거절은
  흔적을 남기지 않으므로(§3.9) 소유자는 거절 사실을 알 수 없다.
- **정원(§3.6) 판정은 수락 시점에 한다.** 대기 중인 초대는 자리를 차지하지 않으므로 정원을
  넘겨 보낼 수 있고, 그 사이 정원이 차면 수락이 거부된다. 거부되어도 초대는 남는다 —
  자리가 나면 같은 초대로 다시 수락할 수 있어야 한다.
- 받은 사람에게 보이는 초대 정보는 **그룹명·초대자 이름·보낸 시각까지다.** 멤버 목록이나 그
  그룹으로 공유된 여행은 수락 전에 보이지 않는다.
- **대기 중인 초대에 담기는 상대 정보도 이름·프로필 사진까지다.** 소유자가 직접 입력한
  이메일이라도 응답으로 되돌려주지 않는다 (§3.1). 누구에게 보냈는지는 이름과 사진으로 충분히
  구분된다.
- **초대는 당사자만 본다.** 보낸 소유자와 받은 사람 외에는 존재하지 않는 것으로 응답한다 (§2.6).

### 3.8 장소 데이터
- 장소 마스터 데이터는 **외부 검색 API로 조회할 뿐 자체 저장 단위로 두지 않는다.**
  장소 테이블도, 장소 단위의 집계도 없다.
- 외부 장소 검색은 안정적인 고유 식별자를 제공하지 않으므로 (§4.2), 기록은 **외부 장소 ID를
  보관하지 않는다.** 대신 검색 결과의 장소명·주소·좌표·원본 링크를 기록에 복사해 저장한다.
- 이 값들은 등록 시점의 **스냅샷**이다. 외부에서 장소 정보가 바뀌어도 이미 만들어진 기록은
  갱신되지 않는다. 기록은 "그때 내가 방문한 곳"을 남기는 것이므로 이 동작이 의도된 것이다.
- 위치는 WGS84 위경도로 다루며 지도 표시에 그대로 쓸 수 있는 정밀도(소수점 6자리 이상)를
  유지한다. 외부 검색 결과의 좌표는 **백엔드가 변환한 뒤** 프론트엔드로 내려준다 (§4.2).

### 3.9 삭제 정책
- 여행과 기록은 **물리 삭제하지 않고 삭제 시각을 남긴다**(soft delete). 통계·이력 보존과
  오삭제 복구를 위해서다. 삭제된 것은 조회에서 제외되며 존재하지 않는 것으로 응답된다.
- **여행을 삭제하면 하위 기록도 함께 삭제된다.** 기록은 여행 없이 존재할 수 없으므로(§3.3)
  남겨 둘 자리가 없다.
- 기록 하나만 삭제하는 것은 여행에 영향을 주지 않는다. 마지막 기록을 지워 기록이 0건이 되어도
  여행은 그대로 남는다 (§3.2).
- **삭제 여부를 나타내는 값은 삭제 시각 하나뿐이다.** 별도의 플래그를 함께 두지 않는다 —
  같은 사실을 두 곳에 적으면 어긋나는 순간 어느 쪽이 맞는지 알 수 없고, 삭제 시각 하나로
  "삭제 여부"와 "삭제 시각"을 모두 답할 수 있다.
- 사진은 파일 본체를 함께 지워야 하므로 soft delete 대상이 아니며 물리 삭제한다.
  삭제된 사진이 어느 여행의 대표 사진이었다면 그 지정이 해제된다 (§3.2).
- 그룹, 그룹 멤버, 공유 관계, 초대는 soft delete 대상이 아니다. 공유 해제와 그룹 탈퇴는
  즉시 조회 권한을 없애야 하므로 흔적을 남기지 않는다. **끝난 초대도 마찬가지로 남기지 않는다** —
  수락·거절·철회 어느 쪽이든 그 초대는 사라지며, 거절 이력을 보관하지 않는 것이 의도된 동작이다
  (§3.7).

## 4. 외부 연동

### 4.1 사용하는 네이버 제품

| 제품 | 발급처 | 용도 | 키 보관 |
|---|---|---|---|
| 네이버 로그인 (OAuth 2.0) | developers.naver.com | 로그인·회원 식별 | 백엔드 전용 |
| 네이버 검색 오픈API – 지역(Local) | developers.naver.com | 등록용 장소 검색의 실제 구현체 | **백엔드 전용** (브라우저 노출 금지) |
| NCP Maps (JS SDK, Geocoder) | ncloud.com | 지도 표시·주소↔좌표 변환 | 프론트엔드 Client ID |

세 가지는 **서로 다른 애플리케이션 등록과 키 발급**이 각각 선행되어야 한다.
키워드 기반 장소 검색은 NCP Maps 단독으로 제공되지 않으므로 지역 검색 오픈API가 반드시 필요하다.

**키는 노출 범위에 맞게 분리 보관한다.** 지역 검색·OAuth 키는 서버에만 두고 브라우저 번들에
포함하지 않으며, 지도 Client ID만 프론트엔드가 갖는다.

### 4.2 지역 검색 오픈API의 구조적 제약 (설계 전제)
아래는 기술 검증으로 확인된 사실이며, 양쪽 모듈의 설계 근거가 된다.

1. **검색어당 최대 5건**만 반환한다 (`display` 최대 5, `start` 사실상 1).
2. **좌표/반경 기반 검색이나 거리순 정렬 파라미터를 지원하지 않는다.**
3. **브라우저에서 직접 호출할 수 없다** (CORS 미지원, 키 노출 문제) → 백엔드 프록시 필수.
4. 응답 좌표(`mapx`, `mapy`)는 WGS84가 아닌 정수형(10^7 배율) 좌표다 → 백엔드가 변환한다.
5. **고유 장소 ID 필드가 없다.** 자체 식별자(해시/UUID) 부여도 검토했으나, 같은 장소가 검색어에 따라
   조금씩 다른 이름·주소로 내려와 안정적 매칭 근거가 되지 못해 **기각되었다.** 대신 원본 링크를
   참고값으로 기록에 보관한다 (§3.8).

**따라서 "장소 검색 결과를 기준 위치 거리순으로 정렬해 여러 건 보여주는" 요구사항은
백엔드 집계로만 충족 가능하다.** 백엔드가 검색어 변형으로 여러 번 호출해 후보를 모으고,
중복 제거 → 좌표 변환 → 거리 계산 → 정렬한 결과를 내려준다. 그럼에도 원본 API의 5건 상한 때문에
후보 풀이 요구 건수에 못 미칠 수 있으며, 이 경우 확보된 만큼만 반환한다. **결과가 적은 것은
오류가 아니다.**

### 4.3 지도 표시
- 지도 표시는 **프론트엔드가 NCP Maps JS SDK로 직접 렌더링**하며 백엔드는 개입하지 않는다.
- Client ID가 설정되지 않은 환경에서는 새 창으로 네이버 지도 검색 결과를 여는 방식으로 대체한다.
- 좌표↔주소 변환이 서버 측에서 필요해지면 백엔드가 Geocoder를 보조적으로 사용할 수 있다.

## 5. 기준 위치와 위치정보 정책

거리순 정렬(목록·장소 검색)의 기준이 되는 사용자 위치는 다음 우선순위로 결정한다.

1. 브라우저 Geolocation API로 현재 위치 획득 (HTTPS 또는 localhost, 사용자 권한 허용 필요)
2. 사용자가 직접 지정한 기준 위치 (지도 클릭 또는 장소 검색으로 선택)
3. 위 둘 다 불가한 경우 **기본 위치는 서울역** (위도 37.5546, 경도 126.9706)

**위치정보법 원칙** — 이용자의 실시간 위치정보를 수집·저장하는 사업은 위치기반서비스사업자에
해당되어 신고 의무 등이 발생할 수 있다. 이를 회피하기 위해 다음을 원칙으로 한다.

- Geolocation으로 얻은 좌표는 **클라이언트에서만, 일시적으로** 사용한다.
- 사용자의 실제 GPS 좌표를 **계정·사용자 식별자와 연결해 저장하지 않는다.** 거리 계산을 위해
  서버로 전달되는 좌표는 요청 파라미터로만 쓰이고 사용자와 묶어 영속화하지 않는다.
- 사용자가 지정한 기준 위치는 브라우저 로컬 저장소에만 보관한다.
- 기록에 저장되는 "장소"의 위치는 장소 정보이지 개인 위치정보가 아니므로 별개다.

실제 신고 대상 해당 여부는 법률 자문으로 별도 확인이 필요하다 (§7).

## 6. 모듈 간 계약

두 모듈이 **같은 값·같은 순서를 지켜야** 어긋나지 않는 항목만 둔다. 요청 형식·응답 스키마·오류
코드 표·쿼리 파라미터·페이지 크기처럼 한쪽이 정의하고 다른 쪽이 따르면 되는 것은 모듈 명세에 있다.

### 6.1 통신과 인증
- 요청·응답은 JSON이며 사진 업로드만 멀티파트다.
- 인증은 **세션 쿠키** 기반이다. 프론트엔드는 모든 API 요청에 자격 증명을 포함해 보낸다.
- 세션 쿠키는 `HttpOnly` 이며 운영 환경에서는 `Secure` 를 적용한다.
- 세션 쿠키 인증이므로 **CSRF 보호를 유지한다.** 서버가 CSRF 토큰 쿠키를 내려주고,
  프론트엔드는 쓰기 요청 시 해당 값을 요청 헤더로 함께 보낸다. 헤더가 없으면 거부된다.
- CORS는 프론트엔드 개발 서버 오리진을 허용하고 자격 증명 포함 요청을 허용한다.

### 6.2 등록의 단계와 중간 실패
**등록 화면 한 번의 제출이 여러 단계로 나뉜다.** 전체를 한 번에 묶는 수단은 없으므로,
중간에 실패했을 때 무엇이 남는지를 양쪽이 같게 이해해야 한다.

| 단계 | 하는 일 | 언제 |
|---|---|---|
| A | 여행 생성 | 새 여행일 때만 |
| 1 | 기록 생성 (소속 여행 지정) | 항상 |
| 2 | 사진 업로드 | 사진을 첨부했을 때만 |

- **기록 생성에는 공개 범위가 없다.** A에서 여행에 이미 정해져 있고 기록은 그것을 따른다 (§3.5).
- **A가 성공하고 1이 실패하면 기록이 0건인 여행이 남는다.** 유효한 상태이므로(§3.2) 되돌리지 않고,
  만들어진 여행으로 이동시켜 기록 등록을 다시 시도하게 한다.
- **1이 성공하고 2가 실패하면 사진이 빠진 기록이 남는다.** 기록 자체는 유효하므로 되돌리지 않고,
  재업로드 동선을 제공한다.
- 대표 사진 지정은 2가 끝난 뒤에만 가능하다. 올라온 사진 중에서 고르는 것이기 때문이다 (§3.2).

### 6.3 사진 업로드 제한 (양쪽 동일 값 유지)

| 항목 | 값 |
|---|---|
| 파일당 최대 크기 | 5MB |
| 한 번에 첨부하는 전체 합계 | 30MB |
| 허용 포맷 | `image/jpeg`, `image/png`, `image/webp` |

- 프론트엔드·백엔드 모두 설정값으로 외부화하며 **같은 값을 유지해야 한다.**
- 클라이언트 검증은 사용자 편의용이고, **최종 검증 책임은 백엔드에 있다.**
- 사진 URL은 저장 방식(파일시스템/오브젝트 스토리지)과 무관하게 항상 접근 가능한 URL로 응답하므로,
  저장소가 전환되어도 프론트엔드 동작은 동일하다.
- ⚠️ **비공개 여행의 사진 URL은 그 자체로 접근 가능하다.** 파일 경로를 아는 사람은 소속 여행의
  공개 범위와 무관하게 이미지를 볼 수 있다는 뜻이며, 현재 단계에서는 추측 불가능한 경로에
  의존한다. 따라서 **"비공개 여행의 사진은 절대 노출되지 않는다"고 오해할 문구를 쓰지 않는다.**
  공개 범위를 파일 서빙까지 강제하는 것은 후속 과제다 (§7).

### 6.4 장소 검색에서 기록으로 넘기는 값
장소명은 **직접 입력할 수 없고** 검색 결과 선택으로만 채워진다. 선택 시 다음 값이 함께 전달되며,
화면에 노출되지 않더라도 등록 요청에 포함되어야 한다.

- 장소명, 주소·도로명주소, 좌표(WGS84), 원본 링크
- 장소 ID는 존재하지 않으므로 보관·전송하지 않는다 (§3.8, §4.2)

## 7. 제약사항 및 향후 과제

도메인 차원의 미결 사항만 둔다. 모듈별 구현 잔여 작업은
[backend §8](./backend/SPECIFICATION.md)과 [frontend §10](./frontend/SPECIFICATION.md)에 있다.

### 7.1 명세와 구현이 어긋나 있는 것
- **초대가 아직 링크 모델로 구현되어 있다** (§3.7). 양쪽 모듈 모두 토큰 URL을 발급·열람하는
  방식이며, 본 개정의 초대 목록 모델은 미반영이다. 대기 중인 링크가 있어도 이관할 대상이
  아니므로 전환 시 그대로 폐기한다 — 링크를 받은 사람이 누구인지 서버가 모르기 때문이다.
- **예산이 소유자에게만 내려간다** (§3.5). 열람자에게도 보이도록 응답에서 제외하지 않는 것이
  본 개정의 변경이며 아직 반영되지 않았다.
- **여행(Trip) 계층이 아직 구현되지 않았다.** 본 개정의 핵심 변경이며 양쪽 모듈 모두 미반영이다.
  기존 기록은 여행 소속이 없으므로 이관이 필요하고, **기록마다 공개 범위가 달랐다면 그 이관은
  무손실이 아니다** — 범위별로 여행을 나눌지 가장 좁은 범위로 수렴시킬지 결정해야 한다.

### 7.2 알려진 한계
- **사진 파일 서빙에 공개 범위가 적용되지 않는다** (§3.4, §6.3). 경로를 아는 사람은 비공개 여행의
  사진도 볼 수 있으며, 현재는 추측 불가능한 경로에만 의존한다. 서명 URL 또는 인가 기반 서빙으로의
  전환이 후속 과제다.
- **초대가 이메일의 가입 여부를 드러낸다** (§3.7). 가입되지 않은 이메일을 구분해 알려주므로,
  이메일을 대입해 보면 그 사람이 이 서비스를 쓰는지 알 수 있다. 오타를 알아차릴 수단과 맞바꾼
  것이며, 문제가 되면 초대 시도 횟수 제한으로 완화한다.
- **장소 검색 후보 풀이 부족할 수 있다** (§4.2). 원본 API의 검색어당 5건 상한 때문에 백엔드
  집계로도 요구 수준을 못 채울 수 있다. 구현 착수 전 재검토하고, 필요 시 다른 POI 데이터
  소스(예: 카카오 로컬 API) 병행을 검토한다.

### 7.3 의도적으로 범위 밖에 둔 것
아래는 "아직 못 한 것"이 아니라 **이번 버전에서 하지 않기로 정한 것**이다. 필요해지는 시점과
그때 무엇이 더 필요한지를 함께 적는다.

| 제외한 것 | 필요해지는 시점 | 그때 추가로 필요한 것 |
|---|---|---|
| **공동 편집** (멤버가 같은 여행을 함께 편집) | 동행자끼리 하나의 여행을 같이 채우고 싶을 때 | 편집 충돌 처리·이력 관리. 그룹이 조회 전용이라는 전제(§3.6)가 깨진다 |
| **공개 링크 공유** (토큰 URL 열람) | 계정 없는 상대에게 비공개 여행을 보여줄 때 | 그룹과 별개 개념. 토큰 수명·폐기 정책 |
| **미가입자 초대** (§3.7) | 아직 가입하지 않은 사람을 미리 초대해 두고 싶을 때 | 이메일 발송 수단과, 주인 없는 대기 초대의 수명·정리 정책 |
| **기록별 방문일** (§3.2) | Day 1 / Day 2 묶음 보기나 일자별 타임라인이 필요할 때 | 기록에 방문 날짜 추가 + 여행 기간 내 검증 |
| **여행의 대표 지역** | 지역별 여행 검색을 넣을 때 | 지금은 하위 기록 주소에서 파생 가능해 중복 입력이 된다 |
| **항목별 경비·정산·통화** (§3.2) | 동행자 간 정산이 필요할 때 | 별도 도메인. 현재 예산은 총액 하나(KRW)뿐 |
| **여행 단위 집계** (카테고리 분포, 평점 요약, 총 이동 거리) | 여행 요약 화면을 만들 때 | 평균 평점은 의도적으로 집계하지 않는다 (§3.3) |
| **사용자 탈퇴(계정 삭제)** (§3.1) | 서비스를 공개 운영할 때 | 소유한 여행·그룹과 타인에게 공유된 기록의 처리 방침 |

### 7.4 외부 확인이 필요한 것
- **위치정보법 해당 여부** — §5 원칙을 지키되 실제 신고 대상인지는 법률 자문으로 확인해야 한다.
- **지역 검색 오픈API 재검증** — 정확한 좌표계와 일일 호출 한도는 구현 착수 시점에 공식 문서로
  다시 확인한다 (§4.2).
